# Sample project (managed repo)

A minimal FastAPI app used as the target of Phase 1 demo tasks
(e.g. TASK-001: add a /health endpoint with a test).
This directory is initialised as its own git repo by `make demo` setup.
